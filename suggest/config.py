class Config:
    def __init__(
            self,
            name: str,
            suggest_endpoint: str|dict,
    ):
        self.name = name
        
        # if isinstance(suggest_endpoint, str):
        #     converter = 
        # self.suggest_endpoint = 
